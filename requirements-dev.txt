# Chi de chay unit test tren may host (khong can Docker):
#   pip install -r requirements-dev.txt
#   $env:PYTHONPATH="src"; python -m pytest tests/ -v
pytest==8.3.3
duckdb==1.1.3
Jinja2==3.1.6
fakeredis==2.25.1
# BAT BUOC: khong co lupa thi fakeredis khong chay duoc EVAL/EVALSHA, test se
# am tham roi vao duong du phong va Lua script KHONG duoc kiem tra lan nao.
lupa==2.8
# Ghim trung voi docker/: redis>=6 gui HELLO khi connect, fakeredis 2.25.1
# khong ho tro -> toan bo test Redis chet voi ResponseError.
redis==5.1.1
# 6.0.3 (khong phai 6.0.2 nhu docker/): 6.0.2 khong co wheel cho Python 3.14
# -> pip build tu source va chet vi duong dan co dau tieng Viet (cp932).
PyYAML==6.0.3
prometheus-client==0.21.0
python-json-logger==2.0.7
# Model uplift (DR-Learner + LightGBM). Can de test duong serving that thay
# vi chi test StubModel.
lightgbm==4.7.0
numpy==1.26.4
# CHI can cho `test_fit_va_xuat_booster` (DRLearner.fit dung StratifiedKFold).
# Thieu chung thi test do SKIP chu khong do — phan thuoc do va phan serving
# van duoc kiem day du bang numpy thuan.
scikit-learn==1.7.2
scipy==1.15.3
confluent-kafka==2.15.0
