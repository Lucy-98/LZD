# Package cho image Airflow. Tat ca deu co wheel dung san cho linux/amd64 va
# linux/arm64 -> khong can gcc/libpq-dev trong image.

# --- lakehouse / transform -------------------------------------------------
duckdb==1.1.3
dbt-core==1.8.7
dbt-duckdb==1.8.3
pyarrow==17.0.0
# pandas/numpy da duoc image Airflow ghim theo constraints cua cac provider.
# Khong nang pandas len 2.2.x vi Google/Snowflake provider yeu cau pandas<2.2.

# --- storage / online store ------------------------------------------------
redis==5.1.1
psycopg2-binary==2.9.9      # BINARY: khong can libpq-dev + gcc
# boto3/botocore da co trong image Airflow va cung duoc constraints dong bo
# voi aiobotocore. Ghim lai rieng o day se lam sai cap botocore cua base image.

# --- streaming -------------------------------------------------------------
confluent-kafka==2.5.3

# --- observability ---------------------------------------------------------
prometheus-client==0.21.0
python-json-logger==2.0.7

# --- tien ich --------------------------------------------------------------
PyYAML==6.0.2
tenacity==9.0.0

# CO Y KHONG CAI:
#   s3fs           - duckdb httpfs + boto3 la du, s3fs keo theo fsspec/aiohttp
#   sqlalchemy     - Airflow da ghim san ban cua no, khong de pip nang cap
#   pydantic       - Airflow da co san
