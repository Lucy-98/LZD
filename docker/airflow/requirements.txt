# Package cho image Airflow. Tat ca deu co wheel dung san cho linux/amd64 va
# linux/arm64 -> khong can gcc/libpq-dev trong image.

# --- lakehouse / transform -------------------------------------------------
duckdb==1.1.3
dbt-core==1.8.7
dbt-duckdb==1.8.3
pyarrow==17.0.0
pandas==2.2.3
numpy==1.26.4

# --- storage / online store ------------------------------------------------
redis==5.1.1
psycopg2-binary==2.9.9      # BINARY: khong can libpq-dev + gcc
boto3==1.35.36

# --- streaming -------------------------------------------------------------
confluent-kafka==2.5.3

# --- observability ---------------------------------------------------------
prometheus-client==0.21.0
python-json-logger==2.0.7

# --- ML tracking -----------------------------------------------------------
# skinny = client thoi. DAG 30 chi log param/metric va goi Registry API.
# `mlflow-skinny` la DU cho `training/register.py`: no chi dung
# `log_artifact` / `log_dict` / `register_model` / `MlflowClient`, khong dung
# `mlflow.<flavor>.log_model` (thu duy nhat can ban day du).
#
# Neu sau nay `train.py` huan luyen TRONG repo (bon TODO(model) con lai) thi
# moi phai doi sang `mlflow==2.16.2` + econml/optuna.
mlflow-skinny==2.16.2

# Model uplift: DRLearner ban giao duoi dang booster text. Airflow can
# lightgbm de VERIFY artifact truoc khi dang ky (so cot, doc duoc hay khong).
lightgbm==4.7.0

# --- tien ich --------------------------------------------------------------
PyYAML==6.0.2
tenacity==9.0.0

# CO Y KHONG CAI:
#   s3fs           - duckdb httpfs + boto3 la du, s3fs keo theo fsspec/aiohttp
#   sqlalchemy     - Airflow da ghim san ban cua no, khong de pip nang cap
#   pydantic       - Airflow da co san
