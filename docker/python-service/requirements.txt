# Dung cho image `lzd/python-service` (producer + consumer + inference API).
# Nguyen tac: chi cai thu 3 service nay THAT SU can luc runtime.
# Moi thu chi dung trong Airflow (duckdb, dbt...) nam o
# docker/airflow/requirements.txt.

# --- API -------------------------------------------------------------------
fastapi==0.115.2
uvicorn[standard]==0.31.1
pydantic==2.9.2

# --- online store + streaming ---------------------------------------------
redis==5.1.1
confluent-kafka==2.5.3

# --- ghi parquet xuong MinIO (stream-consumer) ----------------------------
pyarrow==17.0.0
boto3==1.35.36

# --- suy luan model --------------------------------------------------------
# Model uplift la DRLearner + LightGBM, ban giao duoi dang booster text
# (`models/uplift_voucher/model_booster.txt`, 350 cay, 76 dac trung).
#
# 🚫 KHONG can cloudpickle / scikit-learn / econml. Ban `.pkl` cua model can
#    Python 3.10.9 CHINH XAC (cloudpickle nhung code object), trong khi booster
#    text doc duoc o moi ban Python. Da do: hai ban cho ket qua TRUNG KHIT
#    bit-for-bit (max|lech| = 0.0 tren 3,000 mau), nen khong co ly do de khoa
#    ca image xuong 3.10.
numpy==1.26.4
pandas==2.2.3
lightgbm==4.7.0

# --- observability ---------------------------------------------------------
prometheus-client==0.21.0
python-json-logger==2.0.7

# --- tien ich --------------------------------------------------------------
PyYAML==6.0.2
tenacity==9.0.0

# CO Y KHONG CAI o image nay:
#   duckdb        - chi Airflow doc/ghi warehouse; API doc feature tu Redis
#   pydantic-settings - config doc bang os.environ, khong can them lib
#   s3fs          - boto3 la du de put_object
